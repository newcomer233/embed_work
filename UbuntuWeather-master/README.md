关于运行的问题：我在vscode里面无法通过直接点击运行那个按钮运行，尝试了很多方法，还是无法操作，所以我目前的运行方式是：1.点击运行按钮，不管它的报错2.在vscode的terminal里手动依次输入rm -f weather_fetch     g++ -o weather_fetch weather.cpp -lcurl -ljsoncpp -lpthread     ./weather_fetch      3.回车之后可以在terminal里面看到运行的结果
目前是每30分钟更新一次当前天气状况，每12小时更新一次天气预报，采用的方法在代码的注释里有介绍，gpt说这种方法不用计时器、不阻塞线程，但是我不太确定是否符合要求，请检查
我把运行结果中显示的温度和我手机上的温度做了对比，当前天气以及前两天的天气预报中的温度误差不超过一摄氏度，但是第三天的有很明显的误差，所以在考虑是否要删除第三天的天气预报来确保天气的准确性
